<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppArtista.Views.DetalleAlbumPage"
             BackgroundColor="{StaticResource Primary}"
             Title="Detalle del Álbum">

    <ScrollView Padding="25">
        <VerticalStackLayout Spacing="25">
            <Frame Padding="0" CornerRadius="15" IsClippedToBounds="True" 
                   HasShadow="True" HorizontalOptions="Center" HeightRequest="300" WidthRequest="300">
                <Image Source="{Binding Portada}" Aspect="AspectFill"/>
            </Frame>

            <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
                <Label Text="{Binding Nombre}" 
                       FontSize="28" 
                       FontAttributes="Bold" 
                       TextColor="White" 
                       HorizontalTextAlignment="Center"/>

                <Label Text="{Binding Anio}" 
                       FontSize="20" 
                       TextColor="{StaticResource Secondary}" 
                       HorizontalTextAlignment="Center"/>
            </VerticalStackLayout>

            <BoxView Color="{StaticResource Gray600}" HeightRequest="1" Margin="0,10"/>

            <Label Text="{Binding Descripcion}" 
                   TextColor="White" 
                   FontSize="17" 
                   LineHeight="1.5" 
                   HorizontalTextAlignment="Center"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>