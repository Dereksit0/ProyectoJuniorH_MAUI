namespace AppArtista
{
    // La clase AppShell define la estructura de navegación de la aplicación, utilizando Shell para organizar las páginas y facilitar la navegación entre ellas

    public partial class AppShell : Shell
    {
        public AppShell()
        {
            InitializeComponent();
        }
    }
}
