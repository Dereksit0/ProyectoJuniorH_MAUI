<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="AppArtista.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:AppArtista"
    xmlns:views="clr-namespace:AppArtista.Views"
    Title="AppArtista">

    <TabBar>
        <Tab Title="Inicio" Icon="appicon.png">
            <ShellContent
                Title="Inicio"
                ContentTemplate="{DataTemplate local:MainPage}"
                Route="MainPage" />
        </Tab>

        <Tab Title="Biografía">
            <ShellContent
                Title="Bio"
                ContentTemplate="{DataTemplate views:BioPage}"
                Route="BioPage" />
        </Tab>

        <Tab Title="Discos">
            <ShellContent
                Title="Discografia"
                ContentTemplate="{DataTemplate views:DiscografiaPage}"
                Route="DiscografiaPage" />
        </Tab>

        <Tab Title="Giras">
            <ShellContent
                Title="Giras"
                ContentTemplate="{DataTemplate views:GirasPage}"
                Route="GirasPage" />
        </Tab>
    </TabBar>

</Shell>