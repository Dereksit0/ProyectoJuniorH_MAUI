<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppArtista.Pages.Eventos"
             Title="Eventos">

    <Grid>
        <ListView x:Name="albumsCollectionView"
                  HorizontalOptions="FillAndExpand"
                  VerticalOptions="FillAndExpand"
                  ItemSelected="OnAlbumSelected">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Frame
                            Margin="8"
                            Padding="15"
                            CornerRadius="12"
                            BackgroundColor="#212121">
                            <StackLayout
                                    Padding="12,0,0,0"
                                    Spacing="4">
                                <Label
                                        Text="{Binding Title}"
                                        FontSize="18"
                                        FontAttributes="Bold"
                                        TextColor="White"/>
                                <Label
                                        Text="{Binding ReleaseDate}"
                                        FontSize="13"
                                        TextColor="White"/>
                            </StackLayout>
                        </Frame>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentPage>
